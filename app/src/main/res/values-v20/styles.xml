<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Rain" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/Rain.ActionBar</item>
        <item name="actionBarStyle">@style/Rain.ActionBar</item>
    </style>

    <style name="Rain.ActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/rain_background</item>
        <item name="android:titleTextStyle">@style/Rain.ActionBar.Title</item>

        <item name="background">@color/rain_background</item>
        <item name="titleTextStyle">@style/Rain.ActionBar.Title</item>
    </style>

    <style name="Rain.ActionBar.Title" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/text_white</item>
    </style>

</resources>